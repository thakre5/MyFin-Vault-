# Enable AndroidX libraries (Required for Jetpack Compose & Room)
android.useAndroidX=true

# Automatically migrate third-party dependencies to AndroidX
android.enableJetifier=true

# Optimize Gradle daemon memory for cloud builds
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
